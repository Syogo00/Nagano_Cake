<h2>ジャンル編集</h2>
<%= form_with model: @genre, url: admin_genre_path do |f| %>
  <div class="form-group">
    <%= f.text_field :name %>
  </div>
  <div class="group">
    <%= f.submit "変更を保存" %>
  </div>
<% end %>
